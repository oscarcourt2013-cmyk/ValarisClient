# StellarClient â€” Paper/Purpur server bridge
# Channel: StellarClient:main (must match the Fabric mod)

server-id: elysia-smp
server-name: Elysia SMP

# Minimum protocol accepted from the client (docs/SERVER_API.md)
protocol:
  min: 1
  max: 1
  accept-message: "Welcome to Elysia SMP â€” StellarClient verified"
  reject-message: "Unsupported StellarClient protocol"

database:
  type: sqlite # sqlite | mysql
  sqlite-file: StellarClient.db
  mysql:
    host: 127.0.0.1
    port: 3306
    database: StellarClient
    username: prime
    password: change-me
    pool-size: 8

rewards:
  first_join:
    enabled: true
    message: "<gold>âš¡</gold> <white>Merci d'utiliser <yellow>StellarClient</yellow> ! RÃ©compense dÃ©bloquÃ©e.</white>"
    commands:
      - "eco give %player% 500"
      - "crate key give %player% vote 1"

xp:
  playtime-minutes: 10
  playtime-amount: 5
  login-amount: 2
  notify-client: true

verified:
  badge: "âš¡"
  chat-format: "<gold>âš¡</gold> <gray>%player%</gray> <dark_gray>Â»</dark_gray> <white>%message%</white>"
  tab-prefix: "<gold>âš¡ </gold>"
  require-detection: true
  grant-permission: true

network:
  enabled: false
  # rest | redis
  mode: rest
  rest-url: "http://127.0.0.1:26005"
  rest-token: ""
  redis:
    host: 127.0.0.1
    port: 6379
    password: ""
    channel: StellarClient:network

friends:
  notify-join: true
  message: "<gold>âš¡</gold> <white>Votre ami <yellow>%friend%</yellow> vient de rejoindre le serveur</white>"

notifications:
  first-join-broadcast: false
  broadcast: "<gold>âš¡</gold> <yellow>%player%</yellow> <gray>joue avec StellarClient</gray>"
